for (int i = 0; i < rows; i++) {
    //     for (int j = 0; j < cols; j++) {
    //         g[i * rows + j].real /= rows * cols;
    //         g[i * rows + j].imag /= rows * cols;
    //     }
    // }